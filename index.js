/**
 * Shareable Prettier Configuration
 *
 * Usage in your project's package.json:
 *   { "prettier": "@dorian/prettier-config" }
 *
 * Or in .prettierrc.js for customization:
 *   const baseConfig = require('@dorian/prettier-config');
 *   module.exports = { ...baseConfig, printWidth: 100 };
 */
module.exports = require('./.prettierrc.js');
